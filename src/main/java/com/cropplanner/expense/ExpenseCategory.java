package com.cropplanner.expense;

/** Cost categories a farmer can log against their farm or a specific schedule. */
public enum ExpenseCategory {
    FERTILIZER,
    SEED,
    LABOUR,
    EQUIPMENT,
    OTHER
}
